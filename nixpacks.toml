[phases.setup]
nixPkgs = ["nodejs-18_x", "python311", "python311Packages.pip", "chromium", "chromedriver"]

[phases.install]
cmds = [
  "cd frontend && npm install",
  "cd backend && pip install --user -r requirements.txt"
]

[phases.build]
cmds = [
  "bash build.sh"
]

[start]
cmd = "cd backend && python3 run.py"
